{
  "type": "module",
  "name": "@hazae41/tscousin",
  "description": "Zero-config supply-chain hardened TypeScript relative paths rewriter",
  "version": "1.0.13",
  "scripts": {
    "prepare": "rm -rf ./out && cp -r ./src/. ./itm && deno -A ./itm/mods/tscousin/bin.ts ./itm && tsc",
    "version": "cat deno.json | jq --arg version \"$npm_package_version\" '.version = $version' | tee deno.json > /dev/null && git add deno.json"
  },
  "exports": {
    ".": "./src/mod.ts"
  },
  "bin": {
    "tscousin": "./out/mods/tscousin/bin.js"
  }
}
